#include <stdio.h>

int main() {

    //declarando variáveis
    int i, n;

    //pedindo 'n' ao usuário
    printf("Digite um número inteiro: \n");
    scanf("%d", &n);

    //calculando o produto dos números pares e a soma dos ímpares
    for (i = 1; i <= n; i++) {

        
    }

    return 0;
}